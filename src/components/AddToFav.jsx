import React from "react";
import FavoriteIcon from "@mui/icons-material/Favorite";
const AddToFav = ({ color }) => {
  return <FavoriteIcon style={{ color: color, fontSize: "2rem" }} />;
};

export default AddToFav;
